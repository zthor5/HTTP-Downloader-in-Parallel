from setuptools import setup

setup(name='HttpDownloader',
 version='0.1',
 description='Designed to download a single file in multiple concurrent partitions in parallel.',
 author='ZThor5',
 install_requires=['requests']
)
